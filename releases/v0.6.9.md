# Bestiary Nav 0.6.9

- Expand both Levelling offsets to +1 through +10, including exact +10/+10 targeting.
- Add a monster-group dropdown that updates with the chosen offsets and current BST level. Every matching group in the 44-location ARR catalog is listed with monster name, full spawn-level range, zone, and map coordinates. Catalog coverage currently ends at level 49; this is not a complete database of every game enemy.
- Keep Automatic as the default for all-species combat and automatic area changes. Selecting a group saves that location and restricts new pulls to the matching English game-name IDs, while self-defense remains available. Specific groups do not detour to FATEs or silently switch to another group when unavailable or outgrown. Changing offsets or group stops the current run so the next start uses the chosen settings.
- Enter Levelling recovery as soon as HP reaches zero or the game reports incapacitation, before ordinary combat and event checks. Release owned Rotation Solver control without requiring its former combat mode to remain active; cleanup failures no longer prevent revival attempts.
- Allow manually enabling Levelling while incapacitated when Auto respawn and resume is on. Defer Rotation Solver acquisition until the character has revived and recovered HP. If the dependency is unavailable or another mode is active, show the reason and wait instead of canceling revival.
- Report exact start restrictions, the last active stop reason, and the Return prompt's readiness or ownership check in Copy diagnostics. Retain prompt ownership and enabled-button validation; never accept unrelated confirmations.

The reported live death sequence was not available in the full log. Local regressions reproduce cleanup failures and verify independent recovery steps; live Return confirmation and resumed travel need an in-game check.
