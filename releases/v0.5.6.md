Changes the installer author to **TheKHD5**.

The Beastmaster icon and both original-screenshot previews remain included in the installer metadata. If the installer still shows its old placeholder, refresh its image cache or restart the game to reload the images.

This is a metadata update; gameplay features are unchanged.
