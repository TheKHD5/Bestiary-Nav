Polishes the installer and settings experience.

- New installer icon using the supplied Beastmaster artwork, a shorter description, and two previews from the supplied in-game screenshot.
- Compact settings with `(i)` hover tips beside controls. Travel status and Stop travel appear during active trips.
- `/bnav` opens only Master's Bestiary, even when capture data has already loaded. An open Bestiary stays open.
- `/bnav config` and the installer's Settings button open settings. Selecting a beast or starting travel no longer opens settings.
- Shorter README focused on features, installation, and project files.

The manual-movement cancellation added in 0.5.4 was confirmed working in-game. The new layout and installer presentation need an in-game visual check after updating.

Validation: Release build with zero warnings/errors; all 492 existing checks passed. Installer image dimensions and packaged image URLs were validated.
