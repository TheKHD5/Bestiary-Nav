export default function Home() {
  return <main style={{ maxWidth: 720, margin: '60px auto', padding: 24, lineHeight: 1.7 }}><h1 style={{ fontSize: 30, fontWeight: 650 }}>Bestiary Nav usage reporting</h1><p>This service receives optional usage reports from Bestiary Nav. It does not provide public statistics.</p><h2 style={{ fontSize: 22, marginTop: 28 }}>Your choice</h2><p>Reporting is off by default. Enable or disable it in <strong>/bnav config → Privacy</strong>. When enabled, the plugin sends a random, resettable installation ID and its version about every five minutes while the plugin is loaded. The server records the receipt time.</p><p>No character name, account ID, server, location, combat activity, inventory or other gameplay information is included. A reset ID may count as a new installation.</p><h2 style={{ fontSize: 22, marginTop: 28 }}>Storage and access</h2><p>The maintainer uses private aggregate statistics to understand adoption and support plugin versions. The reporting database stores a hash of the random ID, first and last receipt times, latest plugin version, and daily activity. Records expire after 90 days; cleanup runs when the service next handles traffic. Resetting the local ID does not erase earlier records. Disabling reporting stops future check-ins; a request already in flight may still arrive.</p><p>Network infrastructure necessarily processes IP addresses. The reporting application does not store raw IPs; short-lived keyed hashes are used for rate limiting and expire after ten minutes, with cleanup on subsequent traffic.</p><p><a href="https://github.com/TheKHD5/Bestiary-Nav" style={{ textDecoration: 'underline' }}>Bestiary Nav source and support</a></p></main>;
}
